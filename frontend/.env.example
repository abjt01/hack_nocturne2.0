# HealthBridge Frontend — Environment Config
# Copy to .env if needed; defaults work for development

# Backend API URL (FastAPI on :8000 or mock backend on :8001)
REACT_APP_BACKEND_URL=http://localhost:8001

# App name
REACT_APP_NAME=HealthBridge
